import UIKit

/// The window and root view controller are created by UIKit from the
/// `UISceneStoryboardFile` entry in the scene manifest, so there is nothing to
/// set up here.
class SceneDelegate: UIResponder, UIWindowSceneDelegate {
  var window: UIWindow?
}
